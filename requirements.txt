# Python Dependencies for Fly Sleep Analysis Pipeline
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=1.5.0
numpy>=1.20.0

# Statistical analysis
scipy>=1.11.0  # Required for false_discovery_control in pca_analysis.py
scikit-posthocs>=0.7.0  # Required for post-hoc tests (Tukey HSD, Dunn) in pca_analysis.py
statsmodels>=0.14.0  # Required for Benjamini-Hochberg correction in cluster_characterization.py

# Machine learning
scikit-learn>=1.0.0  # Required for PCA, LinearRegression, DBSCAN, and NearestNeighbors
umap-learn>=0.5.0  # Required for UMAP dimensionality reduction in umap_dbscan_analysis.py
hdbscan>=0.8.33  # Required for HDBSCAN clustering in umap_dbscan_analysis.py
xgboost>=2.0.0  # Death prediction (death_prediction_xgboost.py)
shap>=0.43.0  # SHAP interpretation for XGBoost (death_prediction_xgboost.py)

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# Database (for db-pipeline)
sqlalchemy>=2.0.0  # Required for database ORM and connection management
psycopg2-binary>=2.9.0  # PostgreSQL adapter (binary version for easier installation)
# Note: TimescaleDB is a PostgreSQL extension, not a Python package
# Install TimescaleDB separately: https://docs.timescale.com/install/latest/

# Standard library (no installation needed, but listed for completeness)
# - os, sys, argparse, pathlib, warnings (all built-in)

